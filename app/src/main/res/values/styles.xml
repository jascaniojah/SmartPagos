<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.CustomActionBarTheme"
        parent="@style/Theme.AppCompat">

        Support library compatibility -->
        <item name="actionBarStyle">@style/Theme.CustomActionBarTheme.MyActionBar</item>
        <!--<item name="android:actionBarStyle">@style/MyActionBar</item>-->

    </style>

    <!-- ActionBar styles -->
    <style name="Theme.CustomActionBarTheme.MyActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/grayActionBar </item>
        <!-- Support library compatibility -->
        <item name="background">@color/grayActionBar</item>
    </style>
    <style name="btnStyleBlackpearl" parent="@android:style/Widget.Button">
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:gravity">center</item>
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">0.6</item>
        <item name="android:background">@drawable/custom_btn_black_pearl</item>
        <item name="android:padding">10dip</item>
    </style>
    </resources>
